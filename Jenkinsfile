pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Exemple') {
            steps {
                sh 'ls -l'
            }
        }
    }
}